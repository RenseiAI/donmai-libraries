{
  "name": "@donmai/architectural-intelligence",
  "version": "0.9.0",
  "type": "module",
  "description": "DEPRECATED — Architectural Intelligence (TypeScript). Superseded by the native Go `donmai arch` implementation; preserved for OSS continuity / downstream pins only.",
  "deprecated": "@donmai/architectural-intelligence is deprecated and will be removed in a future major. The architectural-intelligence layer is now native Go inside the donmai binary (use `donmai arch`). This package is preserved for OSS continuity / downstream pins only and receives no further feature work.",
  "author": "Rensei AI (https://rensei.ai)",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RenseiAI/donmai-libraries.git",
    "directory": "packages/architectural-intelligence"
  },
  "homepage": "https://github.com/RenseiAI/donmai-libraries/tree/main/packages/architectural-intelligence",
  "bugs": {
    "url": "https://github.com/RenseiAI/donmai-libraries/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "architectural-intelligence",
    "architecture",
    "patterns",
    "conventions",
    "drift-detection",
    "agent",
    "rensei"
  ],
  "main": "./dist/src/index.js",
  "module": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "peerDependencies": {
    "drizzle-orm": ">=0.40.0",
    "postgres": ">=3.4.0"
  },
  "peerDependenciesMeta": {
    "drizzle-orm": {
      "optional": true
    },
    "postgres": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm clean && pnpm build"
  }
}
